using System;
using System.Text;
using System.Linq;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace HangmanGame
{
    class Program
    {
        static void Main(string[] args)
        {
           //Instantiate random number generator using system-supplied value as seed.
           var rand = new Random();
           //The string Input will store the user input.
           string Input;
           //The string Solution stores the solution word.
           string Solution;
           // The bool HasFinished stores the player status in the game. While false means the the player has not win/finished the game. 
            bool HasFinished = false;
        }
    }
}
